<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="logDir" value="logs"/>
    <property name="maxFileSize" value="10MB"/>
    <property name="maxHistory" value="10"/>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="access-file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logDir}/access.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${logDir}/access.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>${maxHistory}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [ACCESS] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="info-file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logDir}/info.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${logDir}/info.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>${maxHistory}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [INFO] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="exception-file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logDir}/exception.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${logDir}/exception.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>${maxHistory}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [EXCEPTION] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="console"/>
    </root>

    <logger name="x-access" level="INFO" additivity="false">
        <appender-ref ref="access-file"/>
    </logger>

    <logger name="x-info" level="INFO" additivity="false">
        <appender-ref ref="info-file"/>
    </logger>

    <logger name="x-exception" level="ERROR" additivity="false">
        <appender-ref ref="exception-file"/>
    </logger>

</configuration>
